/**
 * @param {Mount} mount
 * @param {ValidInput} input
 * @param {TransactionConfig} config
 *
 * @return {Promise<Transaction> | unknown}
 */
export default function outerHTML(mount: Mount, input?: ValidInput, config?: TransactionConfig): Promise<Transaction> | unknown;
import { Mount } from "./util/types";
import { ValidInput } from "./util/types";
import { TransactionConfig } from "./util/types";
import Transaction from "./transaction";
